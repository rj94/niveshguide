import { PlaceholderPage } from "@/components/dashboard/PlaceholderPage";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Account preferences, theme, and notification settings will be configured here."
    />
  );
}
